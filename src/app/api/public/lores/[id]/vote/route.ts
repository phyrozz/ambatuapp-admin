import { NextResponse } from 'next/server';
import { FieldValue } from 'firebase-admin/firestore';
import { adminDb } from '../../../../../../lib/firebase-admin';
export const runtime = 'nodejs';
const headers = { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': 'content-type' };
export function OPTIONS() { return new NextResponse(null, { headers }); }
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { try { if (!adminDb) throw new Error('Lore service is not configured.'); const { id } = await params; const { anonymousId, value } = await request.json(); if (typeof anonymousId !== 'string' || anonymousId.length < 12 || ![1, -1].includes(value)) throw new Error('Invalid vote.'); const lore = adminDb.collection('lores').doc(id); const vote = lore.collection('votes').doc(anonymousId); const result = await adminDb.runTransaction(async transaction => { const [loreSnapshot, voteSnapshot] = await Promise.all([transaction.get(lore), transaction.get(vote)]); const data = loreSnapshot.data(); if (!data || data.status !== 'Published') throw new Error('Lore not found.'); const prior = voteSnapshot.data()?.value ?? 0; const next = prior === value ? 0 : value; transaction.set(vote, { value: next, updatedAt: FieldValue.serverTimestamp() }); transaction.update(lore, { upvotes: Math.max(0, (data.upvotes ?? 0) + (next === 1 ? 1 : 0) - (prior === 1 ? 1 : 0)), downvotes: Math.max(0, (data.downvotes ?? 0) + (next === -1 ? 1 : 0) - (prior === -1 ? 1 : 0)) }); return next; }); return NextResponse.json({ value: result }, { headers }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : 'Vote failed.' }, { status: 400, headers }); } }
